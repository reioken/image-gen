# Core engine dependencies
httpx>=0.27
aiofiles>=23.2
pydantic>=2.6
python-dotenv>=1.0
tenacity>=8.2
rich>=13.7
Pillow>=10.2
PyYAML>=6.0

# Optional but recommended
aiolimiter>=1.1
pandas>=2.2

# Desktop GUI
PySide6>=6.6

# Packaging to a standalone .exe (run the build on Windows)
pyinstaller>=6.3

# Tests
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
